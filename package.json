{
  "name": "sdkgen",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test --parallel",
    "lint": "lerna run eslint:check --parallel",
    "playground": "lerna run start --scope @sdkgen/playground"
  },
  "devDependencies": {
    "lerna": "^7.3.0"
  }
}
